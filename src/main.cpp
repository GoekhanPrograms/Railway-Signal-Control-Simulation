#include <iostream>

using namespace std;

/**
 *  We will use 20 tracks to simulate a railway system. Two trains will move along these tracks simultaneously.
 */

int main()
{
    int n;
    cout << "Enter a number: ";
    cin >> n;
}